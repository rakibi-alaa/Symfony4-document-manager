<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="stylesheet" href="{{asset('css/css.css')}}" />
        <link rel="stylesheet" href="{{asset('css/font-awesome.min.css')}}" />
        
        
        
        <script type="text/javascript" src="{{asset('js/jquery.js')}}"></script>
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="{{ path('administration') }}">Administration</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                     <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_geodoc') }}">GéoDoc</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('admin_chimie') }}">GéoChimie</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('admin_chrono') }}">GéoChrono</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('admin_ressource') }}">RessourcesMinerale</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('admin_user') }}">Utilisateur</a>
                    </li>
                </ul>

               <div class="form-inline my-2 my-lg-0">
               <ul class="navbar-nav mr-auto">

                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('home') }}">Acceuil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href=""></a>
                    </li>
                    <li class="nav-item">
                        {% if not app.user %}
                            
                        <a class="nav-link" href="{{ path('authentication') }}">Connexion</a>
                        {% else %}
                        <a class="nav-link" href="{{ path('deconnexion') }}">Déconnexion</a> 
                        {% endif %}
                    </li>
                     
                </ul>
    
      
    
    </div>
            </div>
        </nav>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        
        {% block javascripts %}{% endblock %}
    </body>
</html>
